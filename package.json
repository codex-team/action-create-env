{
  "name": "action-create-env",
  "scripts": {
    "build": "node_modules/.bin/ncc build index.js --minify"
  },
  "devDependencies": {
    "@actions/core": "^1.2.6",
    "@vercel/ncc": "^0.25.1"
  }
}
